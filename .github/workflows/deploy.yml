name: Deploy SynTextAI

on:
  push:
    branches: [ master ]
  workflow_dispatch:
    inputs:
      deploy_message:
        description: 'Deployment message'
        required: false
        default: 'Manual deployment'

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    environment: production

    steps:
    - name: Checkout code
      uses: actions/checkout@v7

    - name: Display deployment information
      run: |
        echo "🚀 Building and deploying SynTextAI"
        if [ "${{ github.event_name }}" == "workflow_dispatch" ]; then
          echo "📝 Message: ${{ github.event.inputs.deploy_message }}"
        fi
        
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v4

    # Set Firebase environment variables directly from secrets
    - name: Set Firebase credentials
      run: |
        echo "FIREBASE_PROJECT_ID=${{ secrets.FIREBASE_PROJECT_ID }}" >> $GITHUB_ENV
        echo "FIREBASE_CLIENT_EMAIL=${{ secrets.FIREBASE_CLIENT_EMAIL }}" >> $GITHUB_ENV
        echo "FIREBASE_PRIVATE_KEY=${{ secrets.FIREBASE_PRIVATE_KEY }}" >> $GITHUB_ENV
        echo "FIREBASE_PRIVATE_KEY_ID=${{ secrets.FIREBASE_PRIVATE_KEY_ID }}" >> $GITHUB_ENV
        echo "FIREBASE_CLIENT_ID=${{ secrets.FIREBASE_CLIENT_ID }}" >> $GITHUB_ENV
        echo "FIREBASE_CLIENT_CERT_URL=${{ secrets.FIREBASE_CLIENT_CERT_URL }}" >> $GITHUB_ENV
        # Set default values for optional fields
        echo "FIREBASE_AUTH_URI=https://accounts.google.com/o/oauth2/auth" >> $GITHUB_ENV
        echo "FIREBASE_TOKEN_URI=https://oauth2.googleapis.com/token" >> $GITHUB_ENV 
        echo "FIREBASE_AUTH_PROVIDER_CERT_URL=https://www.googleapis.com/oauth2/v1/certs" >> $GITHUB_ENV
        
    # Create frontend .env file with Firebase web config
    # (Vite only exposes vars prefixed VITE_ to client code; secret names in
    # GitHub are unchanged, only the runtime var names are renamed here)
    - name: Create frontend environment file
      run: |
        cat << EOF > frontend/.env
        VITE_FIREBASE_API_KEY=${{ secrets.REACT_APP_FIREBASE_API_KEY }}
        VITE_FIREBASE_AUTH_DOMAIN=${{ secrets.REACT_APP_FIREBASE_AUTH_DOMAIN }}
        VITE_FIREBASE_PROJECT_ID=${{ secrets.REACT_APP_FIREBASE_PROJECT_ID }}
        VITE_FIREBASE_STORAGE_BUCKET=${{ secrets.REACT_APP_FIREBASE_STORAGE_BUCKET }}
        VITE_FIREBASE_MESSAGING_SENDER_ID=${{ secrets.REACT_APP_FIREBASE_MESSAGING_SENDER_ID }}
        VITE_FIREBASE_APP_ID=${{ secrets.REACT_APP_FIREBASE_APP_ID }}
        VITE_STRIPE_API_KEY=${{ secrets.REACT_APP_STRIPE_API_KEY }}
        VITE_POST_HOG_API_KEY=${{ secrets.REACT_APP_POST_HOG_API_KEY }}
        VITE_GOOGLE_CLIENT_ID=${{ secrets.VITE_GOOGLE_CLIENT_ID }}
        VITE_GOOGLE_API_KEY=${{ secrets.VITE_GOOGLE_API_KEY }}
        EOF
        chmod 600 frontend/.env
    
    # A missing GitHub secret is an empty string, not an error, so a build
    # without these goes green and ships a frontend with the Drive button
    # eliminated as dead code, while the home page copy promising it ships
    # anyway. That happened on 2026-08-12 and cost two deploys to notice,
    # because nothing anywhere said the value was absent.
    #
    # Note for whoever hits this: "Secrets" and "Variables" are different tabs
    # on the same settings page, and a value in the wrong one reads as empty
    # here with no warning.
    - name: Check the browser build values are actually set
      run: |
        missing=""
        [ -z "${{ secrets.REACT_APP_FIREBASE_API_KEY }}" ] && missing="$missing REACT_APP_FIREBASE_API_KEY"
        [ -z "${{ secrets.VITE_GOOGLE_CLIENT_ID }}" ] && missing="$missing VITE_GOOGLE_CLIENT_ID"
        [ -z "${{ secrets.VITE_GOOGLE_API_KEY }}" ] && missing="$missing VITE_GOOGLE_API_KEY"
        if [ -n "$missing" ]; then
          echo "::error::These repository secrets are empty or missing:$missing"
          echo "::error::Settings -> Secrets and variables -> Actions -> Secrets tab (not Variables)."
          exit 1
        fi
        echo "All browser build values are present."

    - name: Login to DockerHub
      uses: docker/login-action@v4
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_TOKEN }}

    - name: Build and push Docker image
      uses: docker/build-push-action@v7
      with:
        context: .
        push: true
        tags: osasdeeon/syntextai:latest
        cache-from: type=registry,ref=osasdeeon/syntextai:buildcache
        cache-to: type=registry,ref=osasdeeon/syntextai:buildcache,mode=max
        build-args: |
          VITE_FIREBASE_API_KEY=${{ secrets.REACT_APP_FIREBASE_API_KEY }}
          VITE_FIREBASE_AUTH_DOMAIN=${{ secrets.REACT_APP_FIREBASE_AUTH_DOMAIN }}
          VITE_FIREBASE_PROJECT_ID=${{ secrets.REACT_APP_FIREBASE_PROJECT_ID }}
          VITE_FIREBASE_STORAGE_BUCKET=${{ secrets.REACT_APP_FIREBASE_STORAGE_BUCKET }}
          VITE_FIREBASE_MESSAGING_SENDER_ID=${{ secrets.REACT_APP_FIREBASE_MESSAGING_SENDER_ID }}
          VITE_FIREBASE_APP_ID=${{ secrets.REACT_APP_FIREBASE_APP_ID }}
          VITE_STRIPE_API_KEY=${{ secrets.REACT_APP_STRIPE_API_KEY }}
          VITE_POST_HOG_API_KEY=${{ secrets.REACT_APP_POST_HOG_API_KEY }}
          VITE_GOOGLE_CLIENT_ID=${{ secrets.VITE_GOOGLE_CLIENT_ID }}
          VITE_GOOGLE_API_KEY=${{ secrets.VITE_GOOGLE_API_KEY }}
          FIREBASE_PROJECT_ID=${{ env.FIREBASE_PROJECT_ID }}
          FIREBASE_PRIVATE_KEY=${{ env.FIREBASE_PRIVATE_KEY }}
          FIREBASE_CLIENT_EMAIL=${{ env.FIREBASE_CLIENT_EMAIL }}
          FIREBASE_PRIVATE_KEY_ID=${{ env.FIREBASE_PRIVATE_KEY_ID }}
          FIREBASE_CLIENT_ID=${{ env.FIREBASE_CLIENT_ID }}
          FIREBASE_CLIENT_CERT_URL=${{ env.FIREBASE_CLIENT_CERT_URL }}
          FIREBASE_AUTH_URI=${{ env.FIREBASE_AUTH_URI }}
          FIREBASE_TOKEN_URI=${{ env.FIREBASE_TOKEN_URI }}
          FIREBASE_AUTH_PROVIDER_CERT_URL=${{ env.FIREBASE_AUTH_PROVIDER_CERT_URL }}

    - name: Copy deploy files to Digital Ocean
      uses: appleboy/scp-action@v0.1.3
      with:
        host: ${{ secrets.DO_HOST }}
        username: ${{ secrets.DO_USERNAME }}
        key: ${{ secrets.DO_SSH_KEY }}
        source: "deploy.sh,docker-compose.yml"
        target: "/home/root/"

    - name: Deploy to Digital Ocean
      uses: appleboy/ssh-action@v0.1.10
      with:
        host: ${{ secrets.DO_HOST }}
        username: ${{ secrets.DO_USERNAME }}
        key: ${{ secrets.DO_SSH_KEY }}
        script_stop: true
        script: |
          cd /home/root/
          
          # Create .env file from GitHub secrets
          echo "${{ secrets.ENV_FILE_CONTENT }}" > .env
          chmod 600 .env
          
          # Create Firebase/GCS service account credentials (JSON) for google-cloud-storage
          # Do not rely on a prebuilt JSON blob secret because newline handling can corrupt JSON.
          mkdir -p /home/root/app/api/config
          export FIREBASE_PROJECT_ID='${{ secrets.FIREBASE_PROJECT_ID }}'
          export FIREBASE_PRIVATE_KEY_ID='${{ secrets.FIREBASE_PRIVATE_KEY_ID }}'
          export FIREBASE_PRIVATE_KEY='${{ secrets.FIREBASE_PRIVATE_KEY }}'
          export FIREBASE_CLIENT_EMAIL='${{ secrets.FIREBASE_CLIENT_EMAIL }}'
          export FIREBASE_CLIENT_ID='${{ secrets.FIREBASE_CLIENT_ID }}'
          export FIREBASE_CLIENT_CERT_URL='${{ secrets.FIREBASE_CLIENT_CERT_URL }}'

          python3 -c "import json, os; creds={'type':'service_account','project_id':os.environ.get('FIREBASE_PROJECT_ID',''),'private_key_id':os.environ.get('FIREBASE_PRIVATE_KEY_ID',''),'private_key':os.environ.get('FIREBASE_PRIVATE_KEY','').replace('\\\\n','\\n'),'client_email':os.environ.get('FIREBASE_CLIENT_EMAIL',''),'client_id':os.environ.get('FIREBASE_CLIENT_ID',''),'auth_uri':'https://accounts.google.com/o/oauth2/auth','token_uri':'https://oauth2.googleapis.com/token','auth_provider_x509_cert_url':'https://www.googleapis.com/oauth2/v1/certs','client_x509_cert_url':os.environ.get('FIREBASE_CLIENT_CERT_URL','')}; missing=[k for k in ('project_id','private_key_id','private_key','client_email','client_id','client_x509_cert_url') if not creds.get(k)]; assert not missing, f'Missing Firebase credential fields: {missing}'; json.dump(creds, open('/home/root/app/api/config/credentials.json','w',encoding='utf-8')); print('Wrote /home/root/app/api/config/credentials.json')"

          chmod 600 /home/root/app/api/config/credentials.json
          
          # Make deploy script executable and run it
          chmod +x deploy.sh
          ./deploy.sh