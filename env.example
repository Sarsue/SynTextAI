# Copy to .env (production) or .env.dev (local) and fill in.
#
# Everything under "Required" must be set. Everything under "Optional" already
# has a working default in code and only needs setting to override it.
#
# VITE_-prefixed values are BAKED INTO THE FRONTEND BUNDLE at build time and can
# be read by anyone who loads the page. Only publishable keys belong there.
# Never give a secret a VITE_ prefix.

# ─── Required: web ──────────────────────────────────────────────────────────

# Comma-separated allowed CORS origins. Falls back to the production domains
# plus localhost dev ports when unset (see api/app.py).
CORS_ORIGINS=https://syntextai.com,https://www.syntextai.com

# Base URL of the frontend, used to build invite links in emails. Falls back to
# the first CORS_ORIGINS entry. Set this to http://localhost:3000 locally, or
# invite links sent while testing will point at production.
APP_URL=https://syntextai.com

# ─── Required: database ─────────────────────────────────────────────────────

DATABASE_NAME=syntextai
DATABASE_USER=dbuser
DATABASE_PASSWORD=xxxx
DATABASE_HOST=db.example.com
DATABASE_PORT=25060
DATABASE_SSLMODE=require
DATABASE_SSLROOTCERT=certs/ca-certificate.crt

# ─── Required: Stripe ───────────────────────────────────────────────────────

STRIPE_SECRET=sk_test_xxxx
STRIPE_ENDPOINT_SECRET=whsec_xxxx
# Publishable key. Shipped to the browser by design.
VITE_STRIPE_API_KEY=pk_test_xxxx

# One graduated tiered price per plan, created in Stripe to match
# api/core/plans.py: Starter is a flat $99 covering 10 seats then $9 each,
# Business a flat $249 covering 30 then $7 each. A Stripe price is immutable,
# so changing an amount means creating a new price and repointing these.
STRIPE_PRICE_ID_STARTER=price_xxxx
STRIPE_PRICE_ID_BUSINESS=price_xxxx

# ─── Required: Firebase ─────────────────────────────────────────────────────

# Client config. Public by design: access is controlled by security rules and
# by the backend verifying the ID token, not by hiding these.
VITE_FIREBASE_API_KEY=xxxx
VITE_FIREBASE_AUTH_DOMAIN=example.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=example
VITE_FIREBASE_STORAGE_BUCKET=example.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=123456
VITE_FIREBASE_APP_ID=1:123456:web:abcdef

# Admin credentials, used to verify ID tokens and to sign document URLs.
# Normally supplied as api/config/credentials.json instead; set these only when
# mounting that file is not an option. FIREBASE_PRIVATE_KEY keeps its literal
# \n escapes.
# FIREBASE_PROJECT_ID=
# FIREBASE_PRIVATE_KEY_ID=
# FIREBASE_PRIVATE_KEY=
# FIREBASE_CLIENT_EMAIL=
# FIREBASE_CLIENT_ID=
# FIREBASE_AUTH_URI=
# FIREBASE_TOKEN_URI=
# FIREBASE_AUTH_PROVIDER_CERT_URL=
# FIREBASE_CLIENT_CERT_URL=

# ─── Required: models ───────────────────────────────────────────────────────
#
# Chat, vision and embeddings all run on DeepInfra, so one key covers all three.
# Everything here speaks the OpenAI-compatible API.
#
# Model ids are namespaced with a slash and are matched exactly. A stray space
# or a missing prefix returns a "model not found" that reads like the model is
# unavailable rather than like a typo.

INFERENCE_BASE_URL=https://api.deepinfra.com/v1/openai
MODEL_ACCESS_KEY=xxxx

MODEL_CHAT_ID=openai/gpt-oss-20b

# Reads a rendered page when the PDF text layer has probably lost it. Chosen by
# measurement against a charging chart whose 48 cells were verified by hand:
# this model reproduced every one, while a smaller, faster one invented digits
# (82/28/78 for a row reading 82/28/80). In a service manual a wrong torque or
# charge value is a safety claim, so accuracy wins. Measure any replacement the
# same way before shipping it.
MODEL_VISION_ID=meta-llama/Llama-4-Maverick-17B-128E-Instruct-FP8

# Embeddings. 1024 dimensions, which is what the pgvector column is declared as
# — a model with a different width needs a schema migration, not just this line.
#
# CHANGING THIS INVALIDATES EVERY STORED VECTOR. An embedding is a set of
# coordinates, and a different model is a different map: old vectors and new
# queries still compare without error, still rank, and return confident
# citations to arbitrary pages. Documents have to be re-uploaded after a change.
DO_EMBEDDINGS_URL=https://api.deepinfra.com/v1/openai
MODEL_EMBEDDING_ID=Qwen/Qwen3-Embedding-0.6B
EMBEDDING_API_KEY=xxxx

# ─── Optional: point vision somewhere else ──────────────────────────────────
# Both default to the values above, so leave them unset while everything is on
# one provider. They exist because vision was on a provider serving it at ~3
# tokens/second against a published median of 123 for the same weights, and
# moving it needed to be two lines rather than a code change.
# VISION_BASE_URL=
# VISION_API_KEY=

# ─── Optional: vision tuning ────────────────────────────────────────────────
# All four have defaults in llm_service.py and need setting only to override.
#
# READ THIS BEFORE TRUSTING THEM. Every value below was measured on 2026-08-13
# against an endpoint writing ~3 tokens/second, and the move to DeepInfra makes
# that arithmetic wrong rather than merely out of date. Re-measure before
# tuning: on a normal provider a page is seconds, not minutes.
#
# Pixels per inch when rendering a page for the model. 150 and 80 were within
# noise of each other on time and both fully accurate, so this is not the lever
# it looks like.
# VISION_DPI=150
#
# Pages read at once. 3 today. Six was measurably WORSE on the old endpoint
# (250s per page delivered against 165s for a single call) because it queued
# server-side rather than running them in parallel. That finding belongs to that
# endpoint and says nothing about this one.
# VISION_CONCURRENCY=3
#
# Wall-clock budget for one page, enforced in the streaming loop rather than by
# the HTTP client: the response is streamed, so httpx's read timeout is the gap
# between chunks and resets on every one. It cannot bound a slow-but-alive
# stream. On deadline the read is discarded rather than truncated, because a
# cut-off table looks exactly like a complete one.
# VISION_DEADLINE=600
#
# A token budget is a time budget. At 3 tokens/second the old 8000 was a
# 44-minute page, and one run really did generate for 33 minutes before being
# killed. The densest real page measured produced ~700 tokens.
# VISION_MAX_TOKENS=2500

# ─── Required: email ────────────────────────────────────────────────────────

SENDGRID_API_KEY=SG.xxxx
# Must be a verified sender or a verified domain in SendGrid, or every send is
# refused and invites go out as links you have to pass on by hand.
SENDGRID_FROM_EMAIL=noreply@yourdomain.com
# Verification key for the SendGrid Event Webhook (Settings > Mail Settings >
# Signed Event Webhook). POST /api/sendgrid/events is public, so without this
# anyone who finds the URL can post plausible-looking delivery events and
# pollute the logs. Set it and forged posts are refused.
SENDGRID_WEBHOOK_PUBLIC_KEY=

# ─── Analytics ──────────────────────────────────────────────────────────────

POST_HOG_API_KEY=phc_xxxx
VITE_POST_HOG_API_KEY=phc_xxxx

# ─── Optional: worker ───────────────────────────────────────────────────────
# Concurrency and lease settings for the background job runner. The defaults in
# api/workers/worker.py are tuned for a single small instance.

# API_BASE_URL=http://localhost:3000   # where the worker posts results back
# QUERY_CONCURRENCY=4                  # questions answered in parallel
# INGEST_CONCURRENCY=2                 # documents ingested in parallel
# MAX_INFLIGHT=6                       # ceiling across both
# MAX_RUNS_PER_USER=3                  # stops one tenant starving the rest
# HEAVY_FILE_BYTES=20971520            # above this, ingestion serialises
# LEASE_MINUTES=15                     # before a stalled run is reclaimed
# POLL_INTERVAL=2
# RECLAIM_INTERVAL=60
# WORKER_ID=worker-1
# WORKER_HEARTBEAT_PATH=/tmp/worker.heartbeat

# ─── Optional: database pool ────────────────────────────────────────────────

# DATABASE_URL=                        # overrides the DATABASE_* parts above
# DB_POOL_SIZE=5
# DB_MAX_OVERFLOW=10
# DB_POOL_TIMEOUT=30
# DB_POOL_RECYCLE=1800
# DB_POOL_PRE_PING=true
# DB_CONNECT_TIMEOUT=30
# DB_CONNECTION_RETRIES=3
# DB_RETRY_DELAY=1
# DB_MAX_RETRY_DELAY=30
# SQL_ECHO=false                       # logs every statement; very noisy

# ─── Optional: retrieval ────────────────────────────────────────────────────

# MAX_TOKENS_CONTEXT=8000              # context budget per answer

# ─── Notifications (Redis) ──────────────────────────────────────────────────
#
# How the API tells the worker there is work, and how the worker tells the API
# to update somebody's browser. Notifications only: the queue itself stays in
# Postgres, so losing Redis costs latency, never jobs.
#
# Leave REDIS_URL empty and both sides fall back to what they did before: the
# worker finds work by polling every POLL_INTERVAL seconds, and notifications
# go over HTTP. Nothing breaks, everything is just slower to start.
# docker-compose sets this to redis://redis:6379/0.

# REDIS_URL=redis://localhost:6379/0

# Proves a call to /api/v1/internal/notify-client really came from the worker.
# That endpoint is the fallback used only when a Redis publish fails, and it
# accepts a user id plus arbitrary event data, so without this anybody who can
# reach the API could push messages into a signed-in customer's browser.
#
# REQUIRED on both the API and the worker, with the same value. Unset means the
# endpoint refuses every call, which loses fallback notifications during a
# Redis outage. Generate one with: openssl rand -hex 32

# INTERNAL_API_SECRET=

# ─── Slack surface ──────────────────────────────────────────────────────────
#
# Answering questions from inside Slack. Unset means the endpoint refuses every
# request, which is the correct default: this is a public URL that answers with
# the contents of private documents.
#
# From the Slack app's Basic Information page.

# SLACK_SIGNING_SECRET=

# ─── Google Drive import ────────────────────────────────────────────────────
#
# Both are build-time values for the browser (VITE_ prefixed), and both are
# public by design: the client id and the API key identify the app, they do not
# authorise anything on their own. Access comes from the customer signing in and
# picking files.
#
# From the Google Cloud project that already backs Firebase:
#   1. Enable "Google Drive API" and "Google Picker API"
#   2. Credentials -> OAuth client ID -> Web application
#      Authorised JavaScript origins: https://syntextai.com (and
#      http://localhost:5173, http://localhost:3000 for development)
#   3. Credentials -> API key. Restrict it to the Picker API.
#
# Leave both unset and the Import from Drive button does not appear at all,
# which is the right default: a button that always fails teaches people the
# feature is broken.

# VITE_GOOGLE_CLIENT_ID=
# VITE_GOOGLE_API_KEY=
