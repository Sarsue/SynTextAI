version: '3.8'

services:
  api:
    environment:
      - DB_POOL_SIZE=3
      - DB_MAX_OVERFLOW=5
      - DB_POOL_RECYCLE=180
      - DB_POOL_PRE_PING=true

    stop_grace_period: 30s
    stop_signal: SIGTERM

  # Add other services as needed
